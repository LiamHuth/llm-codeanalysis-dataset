

Scripts:
- Generate properly formatted training data from source code files and config
- Pre training script that checks token counts against cap, and estimates training cost 
- Post training script that gets training data (loss, etc) from openai


Need:
- A way to compile results and graph data
